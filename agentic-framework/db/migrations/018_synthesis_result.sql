-- Migration 018: Add synthesis_result column to persist the HOSPILOT recommendation
-- (headline, actions, risk, summary) generated by the synthesis node.
ALTER TABLE hospilot_app.sessions
  ADD COLUMN IF NOT EXISTS synthesis_result jsonb;
