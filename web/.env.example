# Web frontend environment template — copy to web/.env for local dev.
# Only VITE_-prefixed vars are exposed to the browser bundle. Do not put secrets here.

# Base URL of the agentic-framework API.
VITE_API_URL=http://localhost:8000
